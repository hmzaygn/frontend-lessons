import React from 'react';
import { Color } from '../../../lib';
export interface TabProps {
    value: any;
    text: string;
    icon?: React.ElementType;
    privateProps?: {
        color: Color;
        isActive: boolean;
        handleTabClick: (value: any) => null;
    };
}
declare const Tab: ({ value, text, icon, privateProps, }: TabProps) => JSX.Element;
export default Tab;
