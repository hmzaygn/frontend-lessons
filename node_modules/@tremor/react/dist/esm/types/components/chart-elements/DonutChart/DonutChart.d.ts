/// <reference types="react" />
import { Color, Height, MarginTop, ValueFormatter } from '../../../lib/inputTypes';
export interface DonutChartDataPoint {
    color?: Color;
}
export interface DonutChartProps {
    data: any[];
    category?: string;
    dataKey?: string;
    colors?: Color[];
    valueFormatter?: ValueFormatter;
    label?: string;
    showLabel?: boolean;
    showAnimation?: boolean;
    showTooltip?: boolean;
    height?: Height;
    marginTop?: MarginTop;
}
declare const DonutChart: ({ data, category, dataKey, colors, valueFormatter, label, showLabel, showAnimation, showTooltip, height, marginTop, }: DonutChartProps) => JSX.Element;
export default DonutChart;
