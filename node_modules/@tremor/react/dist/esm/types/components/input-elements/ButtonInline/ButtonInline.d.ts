import React from 'react';
import { Color, HorizontalPosition, MarginTop, Size } from '../../../lib/inputTypes';
export interface ButtonInlineProps {
    text: string;
    icon?: React.ElementType;
    iconPosition?: HorizontalPosition;
    size?: Size;
    color?: Color;
    handleClick?: {
        (): void;
    };
    marginTop?: MarginTop;
    disabled?: boolean;
}
declare const ButtonInline: ({ text, icon, iconPosition, handleClick, size, color, marginTop, disabled }: ButtonInlineProps) => JSX.Element;
export default ButtonInline;
