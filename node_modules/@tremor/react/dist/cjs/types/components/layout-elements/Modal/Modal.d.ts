import React from 'react';
import { HorizontalPosition, Width } from '../../../lib/inputTypes';
export interface ModalProps {
    showModal: boolean;
    setShowModal: React.Dispatch<React.SetStateAction<boolean>>;
    triggerRef: React.RefObject<HTMLElement>;
    width?: Width;
    maxHeight?: string;
    anchorPosition?: HorizontalPosition;
    children: React.ReactNode;
}
declare const Modal: ({ showModal, setShowModal, triggerRef, width, maxHeight, anchorPosition, children, }: ModalProps) => JSX.Element | null;
export default Modal;
